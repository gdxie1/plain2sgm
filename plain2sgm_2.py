from Tool.open_file import *def file2list(DATA_DIR,FILE_NAME,LANG):    file_in = open_file(DATA_DIR+FILE_NAME+'.'+LANG, 'r')    line_list = [line.strip() for line in file_in]    print 'file list:',len(line_list)    file_in.close()    return line_listif __name__ == '__main__':    DATA_DIR = '/home/vincent/Research/workspace/Ke/'    FILE_NAME = 'sub.pre'    LANG = 'trans'    FINAL_FILE_NAME = FILE_NAME + '.sgm.' + LANG    file_out = open_file(DATA_DIR + FINAL_FILE_NAME, "w")    text = file2list(DATA_DIR, FILE_NAME, LANG)    ID = 1    flag = 0    for line in text:        file_out.write('<p>\n')        file_out.write('<seg id=\"'+str(ID)+'\"> '+line+' </seg>\n')        file_out.write('</p>\n')        ID += 1    file_out.close()